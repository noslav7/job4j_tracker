package ru.job4j.comparator;

/*
public class NameAscCompare implements Comparator<ThenComparingMethod.User> {

    @Override
    public int compare(ThenComparingMethod.User o1, ThenComparingMethod.User o2) {
        return o1.getName().compareTo(o2.getName());
    }
}
*/