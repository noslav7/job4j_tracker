package ru.job4j.stream;

public class CollectToList {
    /*
    public static List<Integer> collect(Stream<Integer> data) {
        List<Integer> result = List.of(data).stream()
                .collect(Collectors,toList()
                        .Function.identity());
        return result;
    }*/
}
