package ru.job4j.stream;

public class CollectToSet {
    /*
       public static Set<Integer> collect(Stream<Integer> data) {
            return Set.of(Stream.of(data)).stream().collect(Collectors.toSet());
        }*/
}
