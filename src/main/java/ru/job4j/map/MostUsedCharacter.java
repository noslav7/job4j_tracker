package ru.job4j.map;

public class MostUsedCharacter {
    /*
    public static char getMaxCount(String str) {
        String lowStr = str.toLowerCase();
        String lowNoSpaces = lowStr.replaceAll(" ", "");
        char[] lowNoSpacesChar = lowNoSpaces.toCharArray();
        Map<Character, Integer> mostUsedTree = new TreeMap<>();
        for (int i = 0; i < lowNoSpaces.length(); i++) {
            mostUsedTree.put(lowNoSpacesChar[i], i);
        }
        int j =0;
        for (int i = 0; i <= mostUsedTree.size(); i++) {
            mostUsedTree.computeIfPresent(i, value -> , value);
        }


        for (int i = 0; i < lowNoSpaces.length(); i++) {
        lowNoSpaces.com
        }

        char rsl = ' ';
        return rsl;
    }*/
}
